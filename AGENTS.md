# AGENTS.md — drop-emulation

Emulator and ROM linking plugin for Drop (#10, #189).

## Commands

```sh
npm ci
npm run build
npm test
npm run typecheck
```

Built on `@droposs/plugin-sdk` (plugin API v2), resolved from the npm registry
(currently `^0.4.0`).
